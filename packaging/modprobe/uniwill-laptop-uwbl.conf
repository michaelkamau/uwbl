# Installed by uwbl. The patched uniwill-laptop module matches the Eluktronics RP-17 by DMI, so
# `force=1` is normally NOT needed. Uncomment the next line only if `dmesg` shows the module
# refusing to load on your machine ("unsupported device") and you accept a tainted kernel.
#options uniwill-laptop force=1
